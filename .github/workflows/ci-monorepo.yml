name: Monorepo CI

on:
  push:
    branches:
      - main
      - master
      - develop
      - 'feature/**'
    paths:
      - 'frontend/**'
      - 'backend/**'
      - 'infra/**'
      - 'docs/**'
      - '.github/workflows/**'

  pull_request:
    branches:
      - main
      - master
      - develop
    paths:
      - 'frontend/**'
      - 'backend/**'
      - 'infra/**'
      - 'docs/**'
      - '.github/workflows/**'

permissions:
  contents: read

jobs:
  detect-changes:
    name: Detect changed areas
    runs-on: ubuntu-latest
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      backend: ${{ steps.filter.outputs.backend }}
      infra: ${{ steps.filter.outputs.infra }}
      docs: ${{ steps.filter.outputs.docs }}
      docs_only: ${{ steps.docs_only.outputs.docs_only }}
      affected: ${{ steps.affected.outputs.affected }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect files
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            frontend:
              - 'frontend/**'
            backend:
              - 'backend/**'
            infra:
              - 'infra/**'
            docs:
              - 'docs/**'

      - name: Detect docs only
        id: docs_only
        shell: bash
        run: |
          if [[ "${{ steps.filter.outputs.docs }}" == "true" && \
                "${{ steps.filter.outputs.frontend }}" == "false" && \
                "${{ steps.filter.outputs.backend }}" == "false" && \
                "${{ steps.filter.outputs.infra }}" == "false" ]]; then
            echo "docs_only=true" >> "$GITHUB_OUTPUT"
          else
            echo "docs_only=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build affected list
        id: affected
        shell: bash
        run: |
          affected=""
          if [[ "${{ steps.filter.outputs.frontend }}" == "true" ]]; then
            affected="${affected} frontend"
          fi
          if [[ "${{ steps.filter.outputs.backend }}" == "true" ]]; then
            affected="${affected} backend"
          fi
          if [[ "${{ steps.filter.outputs.infra }}" == "true" ]]; then
            affected="${affected} infra"
          fi
          if [[ "${{ steps.filter.outputs.docs }}" == "true" ]]; then
            affected="${affected} docs"
          fi

          affected="$(echo "$affected" | xargs)"
          echo "affected=$affected" >> "$GITHUB_OUTPUT"

  frontend:
    name: Frontend pipeline
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend == 'true'
    uses: ./.github/workflows/reusable-component.yml
    with:
      component: frontend
      working-directory: ./frontend
      command: |
        echo "lint frontend"
        echo "test frontend"

  backend:
    name: Backend pipeline
    needs: detect-changes
    if: needs.detect-changes.outputs.backend == 'true'
    uses: ./.github/workflows/reusable-component.yml
    with:
      component: backend
      working-directory: ./backend
      command: |
        echo "lint backend"
        echo "test backend"

  infra:
    name: Infra pipeline
    needs: detect-changes
    if: needs.detect-changes.outputs.infra == 'true'
    uses: ./.github/workflows/reusable-component.yml
    with:
      component: infra
      working-directory: ./infra
      command: |
        echo "terraform fmt -check"
        echo "terraform validate"

  docs-report:
    name: Docs only report
    needs: detect-changes
    if: needs.detect-changes.outputs.docs_only == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Documentation-only change detected
        run: echo "Only documentation changed"

  summary:
    name: Final summary
    needs:
      - detect-changes
      - frontend
      - backend
      - infra
      - docs-report
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Generate report
        shell: bash
        run: |
          echo "## Monorepo report" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- Affected components: ${{ needs.detect-changes.outputs.affected }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Frontend pipeline executed: ${{ needs.detect-changes.outputs.frontend }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Backend pipeline executed: ${{ needs.detect-changes.outputs.backend }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Infra pipeline executed: ${{ needs.detect-changes.outputs.infra }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Docs pipeline executed: ${{ needs.detect-changes.outputs.docs_only }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "### Omitted jobs" >> "$GITHUB_STEP_SUMMARY"

          if [[ "${{ needs.detect-changes.outputs.frontend }}" != "true" ]]; then
            echo "- Frontend omitted" >> "$GITHUB_STEP_SUMMARY"
          fi

          if [[ "${{ needs.detect-changes.outputs.backend }}" != "true" ]]; then
            echo "- Backend omitted" >> "$GITHUB_STEP_SUMMARY"
          fi

          if [[ "${{ needs.detect-changes.outputs.infra }}" != "true" ]]; then
            echo "- Infra omitted" >> "$GITHUB_STEP_SUMMARY"
          fi

          if [[ "${{ needs.detect-changes.outputs.docs_only }}" != "true" ]]; then
            echo "- Docs-only job omitted" >> "$GITHUB_STEP_SUMMARY"
          fi
